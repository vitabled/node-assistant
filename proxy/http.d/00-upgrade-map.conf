# http-level map for the WebSocket upgrade dance (a `map` cannot live inside a
# server block, so it gets its own file in conf.d — which nginx includes in http{}).
# Non-upgrade requests get `Connection: close` instead of a nonsensical `upgrade`.
map $http_upgrade $connection_upgrade {
    default upgrade;
    ''      close;
}
