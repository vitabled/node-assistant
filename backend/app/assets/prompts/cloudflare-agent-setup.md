<!--
  Cloudflare Agent Setup prompt — VENDORED ASSET (placeholder).
  Source: https://developers.cloudflare.com/agent-setup/prompt.md
  © Cloudflare. Content licensed under CC-BY-4.0 (attribution required;
  indicate if changes were made). The Cloudflare name/logos are trademarks and
  are NOT licensed — this asset uses a neutral name and no branding.

  This is a PLACEHOLDER. The upstream text was not vendored verbatim here.
  To activate: fetch the URL above and replace everything below this comment
  with the upstream Markdown VERBATIM, keeping this attribution header.
  The preset is marked `unavailable` in PRESETS.json until that is done.
-->

Пресет «Cloudflare platform setup» ещё не вендорен.

Чтобы активировать: скачайте официальный промпт по адресу
https://developers.cloudflare.com/agent-setup/prompt.md и вставьте его текст
в этот файл ДОСЛОВНО (сохранив шапку-атрибуцию выше), затем снимите флаг
`unavailable` у пресета `cloudflare-agent-setup` в PRESETS.json. Текст промпта
выдумывать нельзя — только вендорить из источника.
